<% if @notifications.exists? %>
  <p>
    <%= link_to "全て既読にする", update_all_notification_path(@notifications), method: :patch, data: { confirm: "全て既読にしますか？" } %>
  </p>
  <%= render partial: "notification", collection: @notifications %>
<% else %>
  <p>通知はありません。</p>
<% end %>
